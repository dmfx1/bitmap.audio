---
import Layout from '../layouts/Layout.astro';
import { Button } from "@/components/ui/button";

// The 11 sections mapped strictly for the sidebars
const sections = [
  { id: "00", label: "Status" },
  { id: "01", label: "Context" },
  { id: "02", label: "Architecture" },
  { id: "03", label: "Neurology.Processing" },
  { id: "04", label: "Neurology.Memory" },
  { id: "05", label: "Metrics" },
  { id: "06", label: "Conversion" },
  { id: "07", label: "Credibility" },
  { id: "08", label: "Perception" },
  { id: "09", label: "Culture" },
  { id: "10", label: "Belief" },
  { id: "11", label: "Execution" }
];
---

<Layout title="The ROI of Audio | bitmap.audio">
  <main class="bg-background text-foreground selection:bg-accent selection:text-foreground">
    
    <div class="fixed inset-0 z-0 pointer-events-none overflow-hidden">
      <div id="parallax-grid" class="absolute top-0 left-0 w-full h-[200vh] bitmap-grid opacity-[0.05] will-change-transform"></div>
      
    </div>

    <div id="scroll-track" class="relative w-full h-[1600vh]">
      
      <div id="sidebar-track" class="absolute left-0 top-0 w-24 h-full z-50 pointer-events-none hidden md:flex flex-col">
        {sections.map((sec, index) => (
          <aside class="h-screen w-full border-r border-border/20 bg-background/80 backdrop-blur-md flex flex-col items-center justify-center gap-8 shadow-[20px_0_40px_-15px_rgba(0,0,0,0.5)]">
            <span class="font-mono text-muted-foreground text-[10px] tracking-widest rotate-180 [writing-mode:vertical-lr] uppercase">{sec.label}</span>
            <div class="h-48 w-[1px] bg-border/30 relative">
              <div class="indicator-line absolute top-0 w-[3px] -left-[1px] bg-accent shadow-[0_0_15px_hsl(var(--accent))] origin-top scale-y-0"></div>
            </div>
            <span class="font-mono text-accent text-[10px]">{sec.id}</span>
          </aside>
        ))}
      </div>

      <div class="sticky top-0 w-full h-screen overflow-hidden pointer-events-auto">
        
        <div id="horizontal-strip" class="flex w-max h-full will-change-transform">
          
          <section data-sidebar="0" class="relative w-[90vw] md:w-[75vw] h-full flex-shrink-0 flex flex-col justify-center pl-4 pr-4 md:pl-48 md:pr-16 group bg-background z-10 mobile:pl-0">
            <!-- Mobile background image — woman reading, bottom-right -->
            <div class="md:hidden absolute bottom-0 right-0 w-[75vw] h-[70vh] pointer-events-none z-0">
              <img src="/images/listening.woman2.emptyBook.png" alt=""
                   class="w-full h-full object-contain object-right-bottom opacity-40" />
            </div>
            <div class="mobile:hidden absolute -right-20 bottom-0 text-[35vw] font-mono font-bold text-muted/5 select-none pointer-events-none transition-transform duration-1000 group-hover:scale-105 z-0">00</div>
            <div class="relative z-10 pl-8 py-2">
              <span class="text-eyebrow text-sm mb-8 mobile:mb-3 block">[ Status // Critical ]</span>
              <h2 class="font-mono text-6xl mobile:text-5xl md:text-[8vw] leading-[0.9] tracking-tighter mb-12 mobile:mb-6 uppercase text-foreground">
                Your brand<br/>is <span class="text-accent drop-shadow-[0_0_20px_hsl(var(--accent)/0.3)]">Muted.</span>
              </h2>
              <div class="w-full max-w-2xl bg-background/60 p-6 border-l-2 border-accent">
                <p class="font-sans text-xl mobile:text-xl md:text-2xl text-muted-foreground leading-relaxed">
                  Visuals reach the eyes, but sound hits the nervous system. Competing on screens alone is a losing battle. It's time to weaponise your audio.
                </p>
              </div>
            </div>
          </section>

          <section data-sidebar="1" class="w-[200vw] md:w-[100vw] h-full flex-shrink-0 flex items-center relative group bg-transparent z-20">
  
            <div class="absolute top-0 -left-[15vw] md:-left-[0vw] w-[60vw] md:w-[50vw] h-full hidden md:flex items-end justify-center pointer-events-none opacity-80 mix-blend-screen z-0">
              <div class="relative w-full max-w-2xl -mb-10 transition-transform duration-1000 group-hover:scale-105">
                <img id="base-hands-layer" src="/images/listening.woman2.emptyHands.png" alt="Woman listening" class="w-full h-auto object-contain object-bottom" />
                <img id="book-fade-layer" src="/images/listening.woman2.emptyBook.png" alt="Book appearing" class="absolute inset-0 w-full h-auto object-contain object-bottom opacity-0" />
                <img id="phone-fade-layer" src="/images/listening.woman2.png" alt="Phone disappearing" class="absolute inset-0 w-full h-auto object-contain object-bottom opacity-0" />
              </div> 
            </div>

            <div class="mobile:hidden absolute right-0 bottom-0 text-[35vw] font-mono font-bold text-muted/5 select-none pointer-events-none transition-transform duration-1000 group-hover:scale-105 z-0">01</div>

            <div id="train-fade-layer" class="absolute bottom-0 right-0 w-[33vw] h-full z-0 opacity-0 pointer-events-none mix-blend-screen flex items-end justify-end md:pr-0">
                <img src="/images/listening.woman2.train.png" alt="Woman and Train" class="w-full h-full object-contain object-right-bottom" />
            </div>

            <div class="w-full flex justify-end mobile:justify-start pr-8 mobile:px-4 md:pr-80 md:pb-52 relative z-10">
                <div class="max-w-2xl border-l-2 border-accent pl-8 py-2 bg-background/20 backdrop-blur-sm mr-0 md:mr-16 relative z-10">
                    <span class="text-eyebrow text-sm mb-8 mobile:mb-3 block text-primary">[ Context // Always_On ]</span>
                    <h2 class="font-mono text-5xl md:text-6xl leading-tight mb-8 mobile:mb-4 uppercase text-foreground">When everyone<br/><span class="text-primary drop-shadow-[0_0_20px_hsl(var(--primary)/0.3)]">is listening.</span></h2>
                    <p class="font-sans text-xl text-muted-foreground leading-relaxed max-w-xl">
                      <span class="text-accent">The screenless audience</span> – 80% of consumers form brand memory via audio cues. If you lack an audio-first identity, you have no presence in the moments when eyes are diverted.
                    </p>
                </div>
            </div>
          </section>

          <section data-sidebar="2" class="w-[200vw] md:w-[100vw] h-full flex-shrink-0 relative border-r border-border/20 bg-primary/80 overflow-hidden group z-10 flex flex-col justify-center mobile:pl-0">


            <div class="absolute right-[-2vw] top-1/2 -translate-y-1/2 w-[55vw] h-[95vh] pointer-events-none z-0 opacity-80
            mobile:left-1/4 mobile:top-1/2 mobile:w-1/2 mobile:h-[100vh] mobile:opacity-30">
              <svg viewBox="50 200 650 800" class="w-full h-full overflow-visible">

                <g class="stroke-accent" fill="none" stroke-width="3" stroke-linecap="square" stroke-linejoin="miter">
                  <path class="data-path" d="M100,300 H200 V400 H100 Z" />
                  <path class="data-path" stroke-width="1.5" stroke="hsl(var(--background))" d="M150,300 V400 M100,350 H200" />
                  <path class="data-path" d="M200,400 H300 V900 H200 Z" />
                  <path class="data-path" d="M250,400 V900" />
                  <path class="data-path" stroke-width="1.5" stroke="hsl(var(--background))" d="M200,500 H300 M200,600 H300 M200,700 H300 M200,800 H300" />
                  <path class="data-path" d="M300,500 H600 V800 H500 V900 H300" />
                  <path class="data-path" d="M300,600 H500 V800 H300" />
                  <path class="data-path" stroke-width="1.5" stroke="hsl(var(--background))" d="M300,550 H600" /> 
                  <path class="data-path" stroke-width="1.5" stroke="hsl(var(--background))" d="M300,850 H500" /> 
                  <path class="data-path" d="M400,500 V600 M400,800 V900" /> 
                  <path class="data-path" d="M500,500 V800" /> 
                  <path class="data-path" d="M500,600 H600 M500,700 H600" /> 
                </g>

                <g class="fill-accent">
                  <circle class="data-node" cx="100" cy="300" r="4" />
                  <circle class="data-node" cx="200" cy="300" r="6" />
                  <circle class="data-node" cx="150" cy="350" r="3" />
                  <circle class="data-node fill-background stroke-accent" stroke-width="2" cx="200" cy="400" r="10" />
                  <circle class="data-node" cx="200" cy="600" r="5" />
                  <circle class="data-node" cx="200" cy="900" r="8" />
                  <circle class="data-node" cx="300" cy="400" r="4" />
                  <circle class="data-node" cx="300" cy="600" r="7" />
                  
                  <circle class="data-node" cx="400" cy="500" r="5" />
                  <circle class="data-node" cx="500" cy="500" r="9" />
                  <circle class="data-node" cx="600" cy="500" r="12" class="fill-background" stroke="hsl(var(--accent))" stroke-width="3" />
                  <circle class="data-node" cx="400" cy="700" r="4" />
                  <circle class="data-node" cx="500" cy="800" r="6" />
                  <circle class="data-node fill-background stroke-accent" stroke-width="2" cx="600" cy="800" r="10" />
                  <circle class="data-node" cx="300" cy="900" r="5" />
                </g>
              </svg>
            </div>

            <div class="pl-4 pr-4 md:pl-72 md:pr-0 relative z-10 w-full max-w-4xl">
              <div class="pl-8 py-2 mobile:border-l-2 mobile:border-accent mobile:ml-0">
                <span class="text-eyebrow text-sm mb-8 mobile:mb-3 block text-background">[ Architecture // Recall ]</span>
                <h2 class="font-mono text-5xl md:text-8xl leading-tight mb-8 mobile:mb-4 uppercase text-foreground">Sound as a<br/><span class="text-accent drop-shadow-[0_0_20px_hsl(var(--primary)/0.3)]">Data Map.</span></h2>
                <p class="font-sans text-xl text-background/80 leading-relaxed max-w-xl">
                  Faster Neurological Processing and Lasting Memory.
                </p>
              </div>
            </div>

          </section>

          <section data-sidebar="3" class="w-[200vw] md:w-[110vw] h-full flex-shrink-0 grid grid-cols-1 md:grid-cols-2 border-r border-border/20 bg-card/30 overflow-hidden relative group z-10 mobile:pl-0">

            <!-- Mobile background image — tiger, bottom-right -->
            <div class="md:hidden absolute bottom-0 right-0 w-[100vw] h-[100vh] pointer-events-none z-0">
              <img src="/images/stalking.sabretooth.png" alt=""
                   class="w-full h-full object-contain object-right-bottom opacity-40" />
            </div>

            <div class="absolute right-30 -bottom-[20vh] w-[100vw] h-full flex items-end justify-end mix-blend-screen pointer-events-none z-0">
               <img id="tiger-focus-layer" src="/images/stalking.sabretooth.png" alt="Sabretooth snapping a twig" 
                    class="w-full h-auto object-contain object-right-bottom opacity-0 blur-[30px] transition-transform duration-1000 group-hover:scale-[1.02]" />
            </div>

            <div class="flex flex-col items-end mobile:items-start justify-center pl-4 pr-4 md:pl-54 md:pr-32 relative z-10 border-l border-border/20 bg-background/20">
              <div class="border-l-2 border-accent bg-background/20 ml-0 md:ml-36 pl-8 py-2">
                <span class="text-eyebrow text-sm mb-8 mobile:mb-3 block text-accent">[ Neurology // Processing_Speed ]</span>
                <h2 class="font-mono text-5xl md:text-6xl leading-tight mb-8 mobile:mb-4 uppercase">Faster than<br/><span class="text-accent">Sight.</span></h2>
                <p class="font-sans text-xl text-muted-foreground leading-relaxed mb-8">
                  Sound is a biological survival mechanism. It reaches the brain 2 to 4 times faster than visual stimuli.<br/><br/>It establishes recognition in just <strong>0.05 seconds</strong> making us aware of our surroundings before we even consciously "see" them.
                </p>
              </div>
            </div>
          </section>

          <section class="hidden md:flex w-[50vw] bg-primary/80 h-full flex-shrink-0 border-r border-border/20 overflow-hidden relative group z-10 flex-col justify-center">
            
            <div class="hidden md:flex flex-col items-center justify-center w-full relative overflow-hidden pointer-events-none z-10 text-center">
               <div class="absolute inset-0 bg-gradient-glow opacity-10"></div>
               <h3 class="font-mono text-[12vw] leading-none text-foreground font-light tracking-tighter drop-shadow-md">
                 8<span class="text-primary">-</span>10<span class="text-2xl tracking-tight text-foreground">ms</span>
               </h3>
               <span class="font-mono text-foreground tracking-[0.5em] uppercase mt-4 drop-shadow-md">Brain Processing Speed</span>
            </div>

          </section>

<section data-sidebar="4" class="w-screen h-full flex-shrink-0 flex flex-col justify-center items-center text-right pl-4 pr-4 md:px-48 relative border-r border-border/20 bg-accent/5 overflow-hidden group z-10 mobile:items-start mobile:text-right">
            <div class="absolute inset-0 radial-gradient-center opacity-20 pointer-events-none"></div>
            <div class="pl-8 py-2 max-w-4xl relative z-10 mobile:border-l-2 mobile:border-accent">
              <span class="text-eyebrow text-accent text-sm mb-8 mobile:mb-3 block">[ Neurology // Echoic_Memory ]</span>

              <h2 class="font-mono text-5xl mobile:text-5xl md:text-[6vw] leading-[1.1] tracking-tighter mb-8 mobile:mb-4 uppercase text-foreground">
                <span id="visuals-fade-text" class="inline-block mobile:opacity-40 mobile:text-[80%]">Visuals fade.</span><br/>
                <span id="sound-grow-text" class="text-primary inline-block origin-right">Sound echoes.</span>
              </h2>

              <p class="font-sans text-2xl mobile:text-xl text-muted-foreground leading-relaxed">
                Auditory sensory memory persists for 2-5 seconds - <strong>10 to 20 times longer</strong> than visual memory. It shows significantly better resistance to decay and triggers emotional encoding via the limbic system.
              </p>
            </div>
          </section>

          <section class="hidden md:flex w-[50vw] bg-foreground/80 h-full flex-shrink-0 border-r border-border/20 overflow-hidden relative group z-10 flex-col justify-center">
            <div class="hidden md:flex flex-col items-center justify-center w-full relative overflow-hidden pointer-events-none z-10 text-center">
               <div class="absolute inset-0 bg-gradient-glow opacity-10"></div>
               <h3 class="font-mono text-[12vw] leading-none text-foreground font-light tracking-tighter drop-shadow-md">
                 20<span class="tracking-tight text-foreground">x</span>
               </h3>
               <span class="font-mono text-background/50 tracking-[0.5em] uppercase mt-4 drop-shadow-md">Longer Memory Retention</span>
            </div>
          </section>

          <section id="metrics-section" data-sidebar="5" class="w-[120vw] h-full flex-shrink-0 hidden md:flex items-center justify-start pl-4 pr-4 md:pl-48 relative border-r border-border/20 bg-card/20 overflow-hidden group z-10">
            <div class="w-full max-w-5xl border-l-2 border-accent pl-8 py-2">
              <span class="text-eyebrow text-sm mb-8 block">[ Metrics // Awareness ]</span>
              
              <h2 class="font-mono text-5xl md:text-7xl leading-[0.9] tracking-tighter mb-16 uppercase text-foreground relative inline-block">
                Recall under<br/>
                <span class="text-accent relative inline-block">
                  Pressure.
                  <div id="pressure-line" class="absolute bottom-0 left-0 h-[4px] bg-accent w-0 origin-left"></div>
                </span>
              </h2>
              
              <div class="relative w-full h-[250px] mt-8">
                
                <div class="stat-card absolute left-0 top-0 w-[280px] h-full border border-border/50 p-8 bg-background/90 backdrop-blur hover:border-accent transition-colors z-[4]">
                  <span class="font-mono text-4xl text-accent block mb-4">191%</span>
                  <p class="font-sans text-muted-foreground text-sm leading-relaxed">Lift in brand awareness within the first two seconds of exposure.</p>
                </div>
                
                <div class="stat-card absolute left-0 top-0 w-[280px] h-full border border-border/50 p-8 bg-background/90 backdrop-blur hover:border-accent transition-colors z-[3]">
                  <span class="font-mono text-4xl text-accent block mb-4">12x</span>
                  <p class="font-sans text-muted-foreground text-sm leading-relaxed">More efficient at driving business results than visual-only advertising.</p>
                </div>
                
                <div class="stat-card absolute left-0 top-0 w-[280px] h-full border border-border/50 p-8 bg-background/90 backdrop-blur hover:border-accent transition-colors z-[2]">
                  <span class="font-mono text-4xl text-accent block mb-4">30%</span>
                  <p class="font-sans text-muted-foreground text-sm leading-relaxed">Brand recall delivered even during "highly distracted" environments.</p>
                </div>
                
                <div class="stat-card absolute left-0 top-0 w-[280px] h-full border border-border/50 p-8 bg-background/90 backdrop-blur hover:border-accent transition-colors z-[1]">
                  <span class="font-mono text-4xl text-accent block mb-4">78%</span>
                  <p class="font-sans text-muted-foreground text-sm leading-relaxed">Recall rate when listeners are actively tuned into audio advertising.</p>
                </div>
              </div>
            </div>
          </section>

          <!-- Mobile: Recall Under Pressure heading slide -->
          <section class="w-screen h-full flex-shrink-0 md:hidden flex flex-col items-center justify-center text-center px-8 bg-card/20 border-r border-border/20 relative z-10">
            <span class="text-eyebrow text-sm mb-6 block">[ Metrics // Awareness ]</span>
            <h2 class="font-mono text-[13vw] leading-[0.9] tracking-tighter uppercase text-foreground">
              Recall Under<br/><span class="text-accent">Pressure.</span>
            </h2>
          </section>

          <!-- Mobile-only: Recall under Pressure stat slides (one per card) -->
          <section class="w-screen h-full flex-shrink-0 md:hidden flex flex-col items-center justify-center text-center px-8 bg-card/20 border-r border-border/20 relative z-10">
            <span class="font-mono text-accent text-[10px] tracking-widest uppercase mb-8 block opacity-60">[ Metrics // Awareness ]</span>
            <span class="font-mono text-[24vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.5)]">191%</span>
            <div class="w-12 h-[2px] bg-accent/40 my-8"></div>
            <p class="font-sans text-muted-foreground text-xl leading-relaxed max-w-xs">Lift in brand awareness within the first two seconds of exposure.</p>
          </section>

          <section class="w-screen h-full flex-shrink-0 md:hidden flex flex-col items-center justify-center text-center px-8 bg-background border-r border-border/20 relative z-10">
            <span class="font-mono text-accent text-[10px] tracking-widest uppercase mb-8 block opacity-60">[ Metrics // Efficiency ]</span>
            <span class="font-mono text-[24vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.5)]">12x</span>
            <div class="w-12 h-[2px] bg-accent/40 my-8"></div>
            <p class="font-sans text-muted-foreground text-xl leading-relaxed max-w-xs">More efficient at driving business results than visual-only advertising.</p>
          </section>

          <section class="w-screen h-full flex-shrink-0 md:hidden flex flex-col items-center justify-center text-center px-8 bg-card/20 border-r border-border/20 relative z-10">
            <span class="font-mono text-accent text-[10px] tracking-widest uppercase mb-8 block opacity-60">[ Metrics // Recall ]</span>
            <span class="font-mono text-[24vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.5)]">30%</span>
            <div class="w-12 h-[2px] bg-accent/40 my-8"></div>
            <p class="font-sans text-muted-foreground text-xl leading-relaxed max-w-xs">Brand recall delivered even during highly distracted environments.</p>
          </section>

          <section class="w-screen h-full flex-shrink-0 md:hidden flex flex-col items-center justify-center text-center px-8 bg-background border-r border-border/20 relative z-10">
            <span class="font-mono text-accent text-[10px] tracking-widest uppercase mb-8 block opacity-60">[ Metrics // Engagement ]</span>
            <span class="font-mono text-[24vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.5)]">78%</span>
            <div class="w-12 h-[2px] bg-accent/40 my-8"></div>
            <p class="font-sans text-muted-foreground text-xl leading-relaxed max-w-xs">Recall rate when listeners are actively tuned into audio advertising.</p>
          </section>

          <section id="roi-section" data-sidebar="6" class="w-[120vw] h-full flex-shrink-0 grid grid-cols-1 md:grid-cols-2 border-r border-border/20 bg-background overflow-hidden relative group z-10 mobile:pl-0">
            <div class="w-5/6 mobile:w-full flex flex-col justify-center pl-4 pr-4 md:pl-48 md:pr-16 relative z-10">
              <div class="ml-0 md:ml-12 pl-6 mobile:pl-4 py-2">
                <span class="text-eyebrow text-sm mb-8 mobile:mb-3 block">[ Capital // Conversion ]</span>
                <h2 class="font-mono text-5xl mobile:text-5xl md:text-6xl leading-[1.1] mb-8 mobile:mb-4 uppercase flex flex-col items-start">
                  <span>Amplified</span>
                  <span id="roi-word" class="text-accent md:text-8xl mobile:text-5xl inline-block origin-top-left leading-none mt-2">ROI.</span>
                </h2>
                <p class="font-sans text-xl mobile:text-xl text-muted-foreground leading-relaxed mb-6 mobile:mb-3">
                  Bespoke sonic assets don't just increase views - they drive behaviour. We see a significant lift in both purchase intent and pricing power.
                </p>
                <div class="border-l-2 border-accent pl-6 mt-4 mobile:hidden">
                  <p class="font-sans text-xl text-foreground leading-relaxed">
                    Allocating 11% of a media budget to audio paired with high "Creative Fluency" can <strong>quadruple</strong> the total campaign ROI.
                  </p>
                </div>
              </div>
            </div>
            
            <div class="hidden md:flex flex-col items-center justify-center text-center bg-primary/10 border-l border-primary/20 relative overflow-hidden">
               
               <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
                 <div class="roi-wave absolute w-[10vw] h-[10vw] rounded-full border border-primary/40"></div>
                 <div class="roi-wave absolute w-[10vw] h-[10vw] rounded-full border border-primary/20"></div>
                 <div class="roi-wave absolute w-[10vw] h-[10vw] rounded-full border border-primary/10"></div>
               </div>

               <div id="roi-text-wrapper" class="relative flex items-center justify-center h-[20vw] w-full mt-12">
                   
                   <h3 class="roi-ghost absolute font-mono text-[24vw] leading-none text-primary/20 blur-[24px] font-bold tracking-tighter mix-blend-screen">4.0x</h3>
                   <h3 class="roi-ghost absolute font-mono text-[21vw] leading-none text-primary/50 blur-[12px] font-bold tracking-tighter mix-blend-screen">4.0x</h3>
                   
                   <h3 class="relative z-10 font-mono text-[18vw] leading-none text-primary font-bold tracking-tighter drop-shadow-[0_0_40px_hsl(var(--primary)/0.6)]">
                     <span id="roi-counter">1.0</span><span class="text-[8vw] text-foreground ml-2">x</span>
                   </h3>
               </div>
               
               <span class="font-mono text-foreground tracking-[0.5em] uppercase mt-4 z-10 relative">Total ROI Multiplier</span>
            </div>

          </section>

          <!-- Mobile ROI multiplier slide -->
          <section class="md:hidden w-screen h-full flex-shrink-0 flex flex-col items-center justify-center bg-primary/10 border-r border-border/20 relative z-10">
            <div class="relative flex items-center justify-center">
              <h3 class="absolute font-mono text-[32vw] leading-none text-primary/20 blur-[20px] font-bold tracking-tighter mix-blend-screen">4.0x</h3>
              <h3 class="absolute font-mono text-[28vw] leading-none text-primary/40 blur-[10px] font-bold tracking-tighter mix-blend-screen">4.0x</h3>
              <h3 class="relative z-10 font-mono text-[24vw] leading-none text-primary font-bold tracking-tighter drop-shadow-[0_0_40px_hsl(var(--primary)/0.6)]">
                4.0<span class="text-[12vw] text-foreground ml-2">x</span>
              </h3>
            </div>
            <span class="font-mono text-foreground/60 tracking-[0.5em] uppercase text-xs mt-8">Total ROI Multiplier</span>
          </section>

          <section id="success-section" data-sidebar="7" class="w-[180vw] h-full flex-shrink-0 grid grid-cols-1 md:grid-cols-2 border-r border-border/20 bg-foreground/90 overflow-hidden relative group z-10">
            <div class="flex flex-col justify-center pl-4 pr-4 md:pl-16 md:pr-32 relative z-10">
              <div class="w-full md:w-1/2 border-l-2 border-accent ml-0 md:ml-72 pl-8 py-2">
                <span class="text-eyebrow text-sm text-background/80 mb-8 mobile:mb-3 block">[ UX // Credibility ]</span>
                <h2 class="font-mono text-5xl mobile:text-5xl md:text-6xl leading-[1.1] mb-8 mobile:mb-4 uppercase flex flex-col items-start">
                  <span id="subconscious-word" class="bg-gradient-to-r from-foreground/10 via-foreground to-foreground/10 bg-clip-text text-transparent bg-[length:200%_auto] bg-[position:200%_center] inline-block">
                    Subconscious
                  </span>
                  <span class="md:text-8xl leading-none font-bold text-background/80 mt-2">Trust.</span>
                </h2>
                <p class="font-sans text-xl mobile:text-xl text-muted-foreground leading-relaxed mb-8 mobile:mb-4">
                  Sound implies physical reality, making digital interactions feel safer. In high-stakes systems, auditory UX cues increase task success rates by 25%.
                </p>
                <div class="pl-6 mt-4 bg-background/80 backdrop-blur-sm p-4 mobile:hidden">
                  <p class="font-sans text-xl text-foreground leading-relaxed">
                    In banking, users associate transaction success primarily with proprietary sound cues over visual confirmations.
                  </p>
                </div>
              </div>
            </div>

            <div class="hidden md:flex flex-col items-center justify-center bg-background/5 relative border-l border-white/5">
               <div class="relative flex items-center justify-center w-[30vw] h-[30vw]">
                 
                 <svg class="absolute inset-0 w-full h-full -rotate-90 pointer-events-none" viewBox="0 0 100 100">
                   <circle cx="50" cy="50" r="45" stroke="currentColor" stroke-width="2" fill="transparent" class="text-foreground/10" />
                   <circle id="success-ring" cx="50" cy="50" r="45" stroke="currentColor" stroke-width="2" fill="transparent" 
                            stroke-dasharray="282.7" stroke-dashoffset="282.7" 
                            class="text-accent" stroke-linecap="square" />
                 </svg>
                 
                 <div class="text-center relative z-10">
                   <h3 class="font-mono text-[12vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.5)]">
                     <span id="task-success-counter">0</span><span class="text-[4vw] ml-1 text-muted-foreground">%</span>
                   </h3>
                   <span class="font-mono text-background/60 text-base tracking-[0.2em] uppercase block">Task Success Increase</span>
                 </div>
               </div>
            </div>

          </section>

          <!-- Mobile: 25% task success slide -->
          <section class="md:hidden w-screen h-full flex-shrink-0 flex flex-col items-center justify-center bg-foreground/90 border-r border-border/20 relative z-10">
            <div class="relative flex items-center justify-center">
              <h3 class="absolute font-mono text-[32vw] leading-none text-accent/20 blur-[20px] font-bold tracking-tighter mix-blend-screen">25%</h3>
              <h3 class="absolute font-mono text-[28vw] leading-none text-accent/40 blur-[10px] font-bold tracking-tighter mix-blend-screen">25%</h3>
              <h3 class="relative z-10 font-mono text-[24vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.5)]">
                25<span class="text-[12vw] text-background/60 ml-1">%</span>
              </h3>
            </div>
            <span class="font-mono text-background/60 tracking-[0.5em] uppercase text-xs mt-8">Task Success Increase</span>
          </section>

<section data-sidebar="8" class="w-screen h-full flex-shrink-0 flex flex-col justify-center items-center text-right pl-4 pr-4 md:px-48 relative border-r border-border/20 bg-primary/20 overflow-hidden group z-10 mobile:items-start mobile:text-right">
            <div class="absolute inset-0 radial-gradient-center opacity-20 pointer-events-none"></div>
            <div class="pl-8 py-2 max-w-4xl relative z-10 mobile:border-l-2 mobile:border-accent">
              <span class="text-eyebrow text-sm text-accent mb-8 block">[ Perception // The Edge ]</span>
              
              <h2 class="font-mono text-5xl md:text-[6vw] leading-[1.1] tracking-tighter mb-8 uppercase text-foreground">
                <span id="" class="inline-block">Perceived as</span><br/>
                <span id="" class="text-accent inline-block origin-right">Innovators</span>
              </h2>
              
              <p class="font-sans text-2xl mobile:text-xl text-muted-foreground leading-relaxed">
                Consumers inherently view brands that utilise strategic sound as more forward-thinking. Data shows that 60% of consumers believe brands that use sound in their communications are more innovative.
              </p>
            </div>
          </section>

          <section class="hidden md:flex w-[50vw] bg-primary/10 h-full flex-shrink-0 border-l-[10px] border-border/20 overflow-hidden relative group z-10 flex-col justify-center">
            <div class="hidden md:flex flex-col items-center justify-center w-full relative overflow-hidden pointer-events-none z-10 text-center">
               <div class="absolute inset-0 bg-gradient-glow opacity-10"></div>
               <h3 class="flex items-start font-mono w-3/4 text-[2vw] leading-none text-foreground font-light tracking-tighter drop-shadow-md">
                 An untapped opportunity for SME's
               </h3>
               <br/>
               <span class="font-mono w-3/4 text-foreground/50 tracking-[0.6em] uppercase mt-4 drop-shadow-md text-justify leading-[2em]">Sonic branding drives <span class="text-foreground text-lg">loyalty, equity, </span>and <span class="text-foreground text-lg">purchase decisions</span> — yet it remains the exclusive domain of global giants.<br/><br/><span class="text-foreground/80 text-xl">SME's make up 90% of businesses worldwide,</span> but most are leaving audio completely untapped.<br/><br/><span class="text-foreground text-base">That's not a gap.</span><br/><br/><span class="text-accent text-sm">That's an opportunity.</span></span>
            </div>
          </section>

          <section data-sidebar="9" class="relative w-[90vw] md:w-[75vw] h-full flex-shrink-0 flex flex-col justify-center pl-4 pr-4 md:pl-48 md:pr-16 group bg-background z-10 mobile:pl-0">
            <div class="mobile:hidden absolute -right-20 bottom-0 text-[35vw] font-mono font-bold text-muted/5 select-none pointer-events-none transition-transform duration-1000 group-hover:scale-105 z-0">00</div>
            <div class="relative z-10 pl-8 py-2">
              <span class="text-eyebrow text-sm mb-8 mobile:mb-3 block">[ Localisation // Borders ]</span>
              <h2 class="font-mono text-6xl mobile:text-5xl md:text-[6vw] leading-[0.9] tracking-tighter mb-12 mobile:mb-6 uppercase text-foreground">
                Seamless<br/>cultural <span class="text-accent md:text-[8vw] drop-shadow-[0_0_20px_hsl(var(--accent)/0.3)]">Resonance.</span>
              </h2>
              <div class="w-full max-w-2xl backdrop-blur-md bg-background/20 p-6 border-l-2 border-accent">
                <p class="font-sans text-xl mobile:text-xl md:text-2xl text-muted-foreground leading-relaxed">
                  Visual copy requires translation. A well-architected sonic identity scales across borders instantly, requiring zero localization overhead while maintaining absolute brand consistency globally.
                </p>
              </div>
            </div>
          </section>

          <section class="w-screen h-full flex-shrink-0 flex flex-col justify-center items-center mobile:items-start text-left pl-4 pr-4 md:px-48 relative border-r border-border/20 bg-primary/5 overflow-hidden group z-10">
            <div class="pl-8 py-2 z-10 mobile:border-l-2 mobile:border-accent">
              <span class="text-eyebrow text-sm mb-8 block">[ Asset // Ownership ]</span>
              <h2 class="font-mono text-5xl mobile:text-[11vw] md:text-[6vw] leading-[1.1] tracking-tighter mb-8 uppercase text-foreground">
                Stop renting <br/>from the sea<br/>of <span class="text-accent">sameness.</span>
              </h2>
              <p class="font-sans text-2xl text-muted-foreground leading-relaxed max-w-4xl bg-background/60 backdrop-blur-md p-8 border border-border/20 mobile:hidden">
                Short-term custom tracks may win the moment, but they fail to build lasting equity. Leading sonic brands ensure at least <strong>30% of their audio assets are wholly owned</strong>, protecting their mental availability.
              </p>
            </div>
          </section>

          <!-- Mobile-only body text slide for "Stop Renting" -->
          <section class="md:hidden w-screen h-full flex-shrink-0 flex flex-col justify-center px-4 bg-background border-r border-border/20 relative z-10 overflow-hidden">
            <!-- Ghost number background -->
            <div class="absolute right-[-2vw] bottom-[-4vw] font-mono font-bold text-[55vw] leading-none text-muted/5 select-none pointer-events-none">30</div>
            <div class="border-l-2 border-accent pl-8 py-2 relative z-10">
              <span class="font-mono text-accent text-[10px] tracking-widest uppercase mb-6 block opacity-60">[ Asset // Ownership ]</span>
              <div class="font-mono text-[20vw] leading-none text-accent font-bold tracking-tighter drop-shadow-[0_0_30px_hsl(var(--accent)/0.4)] mb-6">
                30<span class="text-[10vw] text-foreground/60">%</span>
              </div>
              <p class="font-sans text-xl text-foreground leading-snug mb-4 font-medium">
                Leading sonic brands ensure at least <span class="text-accent font-bold">30% of their audio assets are wholly owned</span> — protecting long-term mental availability.
              </p>
              <p class="font-sans text-base text-muted-foreground leading-relaxed">
                Short-term tracks win the moment. Owned assets build the brand.
              </p>
            </div>
          </section>

          <section data-sidebar="10" class="w-[100vw] h-full flex-shrink-0 grid grid-cols-1 md:grid-cols-2 border-r border-border/20 bg-accent overflow-hidden relative group z-10 mobile:pl-0">
            <div class="flex flex-col justify-center pl-4 pr-4 md:pl-48 md:pr-16 relative z-10">
              <div class="border-l-2 border-accent pl-8 py-2">
                <span class="text-eyebrow text-sm text-background mb-8 block">[ Belief // Power ]</span>
                <h2 class="font-mono text-5xl md:text-6xl leading-tight mb-8 uppercase">Trust<br/>in the <br/><span class="text-background text-8xl font-bold drop-shadow-[0_0_20px_hsl(var(--primary)/0.3)]">power</span> <br/>of sound.</h2>
                <p class="font-sans text-xl text-foreground leading-relaxed mb-6">
                  and tap into the subconscious.
                </p>
              </div>
              <div>
                
              </div>
            </div>
          </section>

          <section data-sidebar="11" class="w-screen h-full flex-shrink-0 flex flex-col items-center justify-center text-center pl-4 pr-4 mobile:pl-0 md:pl-48 md:pr-32 relative overflow-hidden group bg-background z-10 mobile:justify-start">
             <div class="absolute inset-0 flex items-center justify-center opacity-10 pointer-events-none">
               <div class="w-[60vw] h-[60vw] border border-accent rounded-full animate-ping [animation-duration:4s]"></div>
               <div class="absolute w-[30vw] h-[30vw] border border-primary rounded-full animate-ping [animation-duration:2s] delay-1000"></div>
             </div>

            <div class="mobile:flex-1 mobile:flex mobile:flex-col mobile:justify-center mobile:w-full mobile:px-4">
               <div class="relative z-10 flex flex-col items-end pl-8 py-2 text-right mobile:border-l-2 mobile:border-accent mobile:items-start mobile:text-left">
                  <span class="text-eyebrow text-sm mb-8 block text-accent">[ Execution // Final_Phase ]</span>
                  <h2 class="font-mono text-6xl mobile:text-5xl md:text-[8vw] leading-none tracking-tighter mb-8 mobile:mb-4 uppercase text-primary drop-shadow-[0_0_30px_hsl(var(--accent)/0.3)]">Master the<span class="text-accent"><br/>Signal.</span></h2>
                  <p class="font-sans text-2xl mobile:text-xl text-muted-foreground mb-16 mobile:mb-0 max-w-2xl">
                    You know the data. You know the ROI. Let's architect a bespoke audio identity that converts.
                  </p>
               </div>
             </div>
             <Button variant="default" className="is-electric-primary font-mono uppercase tracking-widest px-12 py-8 text-lg hover:scale-105 transition-all shadow-[0_0_40px_hsl(var(--primary)/0.4)] mobile:w-[calc(100%-3rem)] mobile:mx-6 mobile:mb-12">
               Initiate_Consultation
             </Button>
          </section>

          <section class="w-[20vw] md:w-[25vw] h-full flex-shrink-0 bg-background z-10"></section>

        </div>
      </div>
    </div>
  </main>
</Layout>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  
  gsap.registerPlugin(ScrollTrigger);

  const initStickyScroll = () => {
    const strip = document.querySelector("#horizontal-strip") as HTMLElement;
    const track = document.querySelector("#scroll-track") as HTMLElement;
    const indicators = gsap.utils.toArray(".indicator-line") as HTMLElement[];

    const parallaxGrid = document.querySelector("#parallax-grid");
    // Grab the new fading phone layer
    const phoneFadeLayer = document.querySelector("#phone-fade-layer");
    const bookFadeLayer = document.querySelector("#book-fade-layer"); // NEW
    const baseHandsLayer = document.querySelector("#base-hands-layer"); // NEW
    
    // Grab the new Train layer
    const trainFadeLayer = document.querySelector("#train-fade-layer"); // NEW

    // Grab the new SVG Data Map elements
    const dataPaths = gsap.utils.toArray(".data-path");
    const dataNodes = gsap.utils.toArray(".data-node");

    const visualsFadeText = document.querySelector("#visuals-fade-text"); // NEW
    const soundGrowText = document.querySelector("#sound-grow-text"); // NEW

    // Grab the tiger image
    const tigerFocusLayer = document.querySelector("#tiger-focus-layer");

    // METRICS SECTION ANIMATIONS (Pressure Line & Sliding Cards)
    const metricsSection = document.querySelector("#metrics-section") as HTMLElement;
    const pressureLine = document.querySelector("#pressure-line");
    const statCards = gsap.utils.toArray(".stat-card") as HTMLElement[];

    // AMPLIFIED ROI SECTION ANIMATION
    const roiSection = document.querySelector("#roi-section") as HTMLElement;
    const roiCounter = document.querySelector("#roi-counter") as HTMLElement;
    const roiWrapper = document.querySelector("#roi-text-wrapper");
    const roiGhosts = gsap.utils.toArray(".roi-ghost") as HTMLElement[];
    const roiWaves = gsap.utils.toArray(".roi-wave") as HTMLElement[];
    const roiWord = document.querySelector("#roi-word");
    const ghostCounters = document.querySelectorAll(".ghost-counter");

    //Success Counter Elements
    const successSection = document.querySelector("#success-section") as HTMLElement; // Add 'as HTMLElement'
    const successCounter = document.querySelector("#task-success-counter") as HTMLElement; // Add this too for good measure
    const successRing = document.querySelector("#success-ring") as SVGElement;


    if (!strip || !track) return;

    const isMobile = window.innerWidth < 768;

    if (isMobile) {
      document.documentElement.classList.add('is-mobile-snap');
      // Adjust data map SVG viewBox to better fill 9:16 portrait
      const archSvg = document.querySelector('section[data-sidebar="2"] svg');
      if (archSvg) {
        archSvg.setAttribute('viewBox', '80 250 480 860');
      }
      return;
    }

    // Returns an absolute scroll position as a fraction of the total scrollable range,
    // so all triggers scale correctly regardless of track height.
    const scrollAt = (fraction: number) => (track.offsetHeight - window.innerHeight) * fraction;

    gsap.to(strip, {
      x: () => -(strip.scrollWidth - window.innerWidth),
      ease: "none",
      scrollTrigger: {
        trigger: track,
        start: "top top",
        end: "bottom bottom",
        scrub: 0.5, 
      }
    });

    if (parallaxGrid) {
      gsap.to(parallaxGrid, {
        y: "-50vh", 
        ease: "none",
        scrollTrigger: {
          trigger: track,
          start: "top top",
          end: "bottom bottom",
          scrub: 0.1, 
        }
      });
    }

    // Measure where each sidebar section actually centers horizontally,
    // then resize asides and set indicator triggers to match those real positions.
    const sidebarEl = document.getElementById('sidebar-track') as HTMLElement;
    const sidebarAsides = sidebarEl ? (Array.from(sidebarEl.querySelectorAll('aside')) as HTMLElement[]) : [];
    const sidebarSections = (Array.from(strip.querySelectorAll('[data-sidebar]')) as HTMLElement[])
      .sort((a, b) => parseInt(a.dataset.sidebar!) - parseInt(b.dataset.sidebar!));

    const totalH = strip.scrollWidth - window.innerWidth;
    const totalV = track.offsetHeight - window.innerHeight;

    // Vertical scroll position at which each section is horizontally centered
    const sectionScrollPos = sidebarSections.map(sec => {
      const centerH = sec.offsetLeft + sec.offsetWidth / 2 - window.innerWidth / 2;
      return Math.max(0, (centerH / totalH) * totalV);
    });

    // Resize each aside so its vertical center is at the viewport midpoint
    // exactly when scrollY equals that section's horizontal center position.
    let cumTop = 0;
    sidebarAsides.forEach((aside, i) => {
      if (i >= sectionScrollPos.length) return;
      const targetDocCenter = sectionScrollPos[i] + window.innerHeight / 2;
      const height = Math.max(window.innerHeight * 0.5, 2 * (targetDocCenter - cumTop));
      aside.style.height = `${height}px`;
      cumTop += height;
    });

    // Indicator lines animate across each section's scroll window
    indicators.forEach((ind, i) => {
      if (i >= sectionScrollPos.length) return;
      const start = sectionScrollPos[i];
      const end = i + 1 < sectionScrollPos.length ? sectionScrollPos[i + 1] : totalV;
      gsap.to(ind, {
        scaleY: 1,
        ease: "none",
        scrollTrigger: {
          trigger: track,
          start: `${start}px top`,
          end: `${end}px top`,
          scrub: true,
        }
      });
    });

    window.addEventListener('keydown', (e) => {
      const slideHeight = window.innerHeight;
      if (e.key === 'ArrowRight' || e.key === 'ArrowDown') {
        e.preventDefault();
        window.scrollBy({ top: slideHeight, behavior: 'smooth' });
      } 
      else if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
        e.preventDefault();
        window.scrollBy({ top: -slideHeight, behavior: 'smooth' });
      }
    });

    // THE 3-STEP STAGGERED FADE LOGIC (Woman on Left)
    if (phoneFadeLayer && bookFadeLayer && baseHandsLayer) {
      
      // PHASE 1: Fade the phone OUT quickly
      gsap.fromTo(phoneFadeLayer, 
        { opacity: 1 }, 
        {
          opacity: 0,   
          ease: "none",
          scrollTrigger: {
            trigger: track,
            start: "top -33%",   
            end: "top -35%",    
            scrub: true,
          }
        }
      );

      // PHASE 2A: Fade the book IN
      gsap.fromTo(bookFadeLayer, 
        { opacity: 0 }, 
        {
          opacity: 1,   
          ease: "none",
          scrollTrigger: {
            trigger: track,
            start: "top -35%",  
            end: "top -40%",    
            scrub: true,
          }
        }
      );

      // PHASE 2B: Fade the empty hands OUT at the exact same time
      gsap.fromTo(baseHandsLayer, 
        { opacity: 1 }, 
        {
          opacity: 0,   
          ease: "none",
          scrollTrigger: {
            trigger: track,
            start: "top -40%",  // Exactly matches the book fade IN start
            end: "top -50%",    // Exactly matches the book fade IN end
            scrub: true,
          }
        }
      );
    }

    // NEW Train Image Fade (Context Section - Right Side)
    if (trainFadeLayer) {
      gsap.fromTo(trainFadeLayer,
        { opacity: 0 },
        {
          opacity: 0.8, // Match established mix-blend opacity
          ease: "none",
          scrollTrigger: {
            trigger: track,
            // Start fading in after arriving securely in Section 01
            start: () => `${scrollAt(0.07)}px top`,
            end: () => `${scrollAt(0.12)}px top`,
            scrub: true,
          }
        }
      );
    }

    if (dataPaths.length > 0) {
      
      dataPaths.forEach((path: any) => {
        const length = path.getTotalLength();
        gsap.set(path, { strokeDasharray: length, strokeDashoffset: length });
      });

      // 1. FADE THE NODES (Clean reveal, no 'flying in')
      gsap.fromTo(dataNodes, 
        { opacity: 0 }, // Just start hidden
        {
          opacity: 1, 
          stagger: 0.08, // Subtle sweep
          ease: "none", // Linear fade feels more 'technical'
          scrollTrigger: {
            trigger: track,
            start: () => `${scrollAt(0.11)}px top`,
            end: () => `${scrollAt(0.13)}px top`,
            scrub: true,
          }
        }
      );

      // 2. DRAW THE PATHS (To match the nodes)
      gsap.to(dataPaths, {
        strokeDashoffset: 0, 
        stagger: 0.1, 
        ease: "power1.inOut",
        scrollTrigger: {
          trigger: track,
          start: () => `${scrollAt(0.12)}px top`,
          end: () => `${scrollAt(0.17)}px top`,
          scrub: true,
        }
      });
    }

    // Biologically Faster Sound (Blur to Focus underlay - Tiger Section)
    if (tigerFocusLayer) {
      gsap.fromTo(tigerFocusLayer,
        { 
          opacity: 0, 
          filter: "blur(30px)", // Starts extremely blurry
        },
        {
          opacity: 0.8,         
          filter: "blur(0px)",  // Snaps into sharp focus
          scale: 1,             
          ease: "power2.out",
          scrollTrigger: {
            trigger: track,
            // Trigger 2.8 screens down (As the Processing Speed section slides in)
            start: () => `${scrollAt(0.18)}px top`,
            end: () => `${scrollAt(0.27)}px top`,
            scrub: true,
          }
        }
      );
    }

    // Thematic Fade for "Visuals Fade" text
    if (visualsFadeText) {
      gsap.fromTo(visualsFadeText,
        { opacity: 1 },
        {
          opacity: 0.05, // Fades to barely visible (or set to 0 to completely vanish)
          ease: "none",
          scrollTrigger: {
            trigger: track,
            // Starts fading as the section reaches the center of the screen
            start: () => `${scrollAt(0.27)}px top`,
            end: () => `${scrollAt(0.32)}px top`,
            scrub: true,
          }
        }
      );
    }

    // 1. Your existing scale logic (leave this exactly as is)
    if (soundGrowText) {
      gsap.fromTo(soundGrowText,
        { scale: 1 },
        {
          scale: 1.2, 
          ease: "none",
          scrollTrigger: {
            trigger: track,
            start: () => `${scrollAt(0.275)}px top`,
            end: () => `${scrollAt(0.35)}px top`,
            scrub: true,
          }
        }
      );

      // 2. NEW: The continuous vibration effect
      gsap.to(soundGrowText, {
        x: 2,                 // Move 3px to the right
        duration: 0.05,       // Extremely fast (20 times per second)
        repeat: -1,           // Loop infinitely
        yoyo: true,           // Ping-pong back and forth (0 to 3 to 0 to -3...)
        ease: "sine.inOut",   // Smooth direction changes
        scrollTrigger: {
          trigger: track,
          start: () => `${scrollAt(0.3)}px top`,
          end: () => `${scrollAt(0.6)}px top`,
          // Instead of scrubbing, just play/pause the vibration based on visibility
          toggleActions: "play pause resume pause" 
        }
      });
    }

    if (metricsSection && pressureLine && statCards.length > 0) {
      
      // Helper function
      const getScrollPos = (horizontalPos: number) => {
        const totalHorizontal = strip.scrollWidth - window.innerWidth;
        const totalVertical = track.offsetHeight - window.innerHeight;
        return (horizontalPos / totalHorizontal) * totalVertical;
      };

      // 1. Draw the "Pressure" Line
      gsap.fromTo(pressureLine,
        { width: "0%" },
        {
          width: "100%",
          ease: "none",
          scrollTrigger: {
            trigger: track,
            // Start drawing when section is entering
            start: () => `${getScrollPos(metricsSection.offsetLeft - window.innerWidth * 0.4)}px top`,
            // Finish drawing exactly when centered
            end: () => `${getScrollPos(metricsSection.offsetLeft - window.innerWidth * 0.1)}px top`,
            scrub: true,
          }
        }
      );

      // 2. Slide the Cards out horizontally (Locked to physical screen center)
      if (statCards.length > 1) {
        
        // Calculate the total physical distance the cards will fan out (300px per card)
        const cardOffset = 300;
        const maxDealDistance = (statCards.length - 1) * cardOffset; 
        
        // Create a single timeline to sequence the animations
        const cardTimeline = gsap.timeline({
          scrollTrigger: {
            trigger: track,
            // START: When the base card hits exactly the center of the screen
            start: () => `${getScrollPos(metricsSection.offsetLeft - window.innerWidth * 0.2)}px top`,
            // END: Exactly when the physical location of the final card hits the center
            end: () => `${getScrollPos(metricsSection.offsetLeft + maxDealDistance - window.innerWidth * 0.5)}px top`,
            scrub: 1, 
          }
        });

        // Loop through the cards, starting at index 1 (the second card)
        for (let i = 1; i < statCards.length; i++) {
          
          // Select the current card AND all cards underneath it
          const movingStack = statCards.slice(i); 
          
          // Add a step to the timeline
          cardTimeline.to(movingStack, {
            x: i * cardOffset, // Move the stack out 300px
            ease: "none", // CRITICAL: Use "none" so the movement perfectly tracks your scroll wheel 1:1
          });
        }
      }
    } // Ensure this closing brace is here!

    if (roiSection && roiCounter && roiWrapper) {
      const roiObj = { val: 1.0 };
      
      const getScrollPos = (horizontalPos: number) => {
        const totalHorizontal = strip.scrollWidth - window.innerWidth;
        const totalVertical = track.offsetHeight - window.innerHeight;
        return (horizontalPos / totalHorizontal) * totalVertical;
      };

      // Create a master timeline for the whole "Amplification" sequence
      const roiTl = gsap.timeline({
        scrollTrigger: {
          trigger: track,
          // DELAYED START: Wait until the right column is actually visible
          start: () => `${getScrollPos(roiSection.offsetLeft - window.innerWidth * 0.3)}px top`,
          // DELAYED END: Finish deep into the section so you have time to watch it
          end: () => `${getScrollPos(roiSection.offsetLeft + window.innerWidth * 0.6)}px top`,
          scrub: 1, 
        }
      });

      // 0. NEW: Scale the word "ROI." from small (50%) to full size (100%)
      if (roiWord) {
        roiTl.fromTo(roiWord, 
          { scale: 0.5, opacity: 0.5 }, 
          { scale: 1, opacity: 1, ease: "power2.out" }, 
        0); 
      }

      // 1. The Counter: 1.0 to 4.0
      roiTl.to(roiObj, {
        val: 4.0,
        ease: "none",
        onUpdate: () => {
          const currentText = roiObj.val.toFixed(1); 
          
          // Update the main center number
          roiCounter.innerText = currentText; 
          
          // Update the blurred ghost outlines so they count up in perfect sync!
          roiGhosts.forEach(ghost => {
            ghost.innerText = currentText + "x";
          });
        }
      }, 0);

      // 2. (Main Wrapper scale remains unchanged)
      roiTl.fromTo(roiWrapper, 
        { scale: 0.3, opacity: 0 },
        { scale: 1, opacity: 1, ease: "power2.out" }, 
      0);

      // 3. NEW: The Halo Bloom. Ghosts scale centrally behind the main number.
      roiGhosts.forEach((ghost, i) => {
         // Deep blur (index 0) starts HUGE and completely faint.
         // Mid blur (index 1) starts slightly smaller.
         const startScale = i === 0 ? 3 : 2; 
         
         roiTl.fromTo(ghost,
           { 
            // Pinned centrally (x: 0, y: 0)
            x: 0, y: 0, rotation: 0,
            scale: startScale, // Start huge
            opacity: 0,       // Start invisible
           },
           { 
            scale: 1,         // Settle to their HTML defined large size
            opacity: 1,       // Become visible blurred outlines
            ease: "power3.out" 
           },
         0);
      });

      // 4. (Shockwaves remain unchanged)
      roiWaves.forEach((wave, i) => {
         roiTl.to(wave, {
            width: `${60 + (i * 30)}vw`,
            height: `${60 + (i * 30)}vw`,
            opacity: 0, 
            ease: "power2.out"
         }, 0);
      });
    }

    // SUCCESS COUNTER LOGIC
    if (successCounter && successSection && successRing) {
      
      const getScrollPos = (horizontalPos: number) => {
        const totalHorizontal = strip.scrollWidth - window.innerWidth;
        const totalVertical = track.offsetHeight - window.innerHeight;
        return (horizontalPos / totalHorizontal) * totalVertical;
      };

      // The exact physical scroll window:
      // Starts when the counter enters the right edge (0.5)
      // Ends exactly when it hits dead-center of the screen (0.85)
      const startTrigger = () => `${getScrollPos(successSection.offsetLeft + window.innerWidth * 0.25)}px top`;
      const endTrigger = () => `${getScrollPos(successSection.offsetLeft + window.innerWidth * 0.95)}px top`;

      // 1. Natively animate the SVG Ring
      gsap.fromTo(successRing, 
        { strokeDashoffset: 282.7 },
        {
          strokeDashoffset: 0, // 0 means the stroke covers the entire circle!
          ease: "none",
          scrollTrigger: {
            trigger: track,
            start: startTrigger,
            end: endTrigger,
            scrub: true, 
          }
        }
      );

      // 2. Natively animate the Number using GSAP's text snapping
      gsap.fromTo(successCounter, 
        { innerText: 0 },
        {
          innerText: 25,
          snap: { innerText: 1 }, // Forces GSAP to only render clean whole numbers
          ease: "none",
          scrollTrigger: {
            trigger: track,
            start: startTrigger,
            end: endTrigger,
            scrub: true, 
          }
        }
      );
    }
  };

  document.addEventListener('astro:page-load', initStickyScroll);
  if (document.readyState === 'complete') initStickyScroll();
  else window.addEventListener('load', initStickyScroll);

</script>

<style is:global>
  html.is-mobile-snap {
    scroll-snap-type: y mandatory;
  }

  html.is-mobile-snap #scroll-track {
    height: auto !important;
  }

  html.is-mobile-snap #scroll-track > .sticky {
    position: static !important;
    height: auto !important;
    overflow: visible !important;
  }

  html.is-mobile-snap body {
    overflow-x: hidden;
  }

  html.is-mobile-snap #horizontal-strip {
    flex-direction: column !important;
    width: 100vw !important;
    height: auto !important;
  }

  html.is-mobile-snap #horizontal-strip > section {
    width: 100vw !important;
    max-width: 100vw !important;
    height: 100dvh !important;
    min-height: 100dvh;
    scroll-snap-align: start;
    scroll-snap-stop: always;
    /* REMOVED: display, flex-direction, justify-content — was breaking hidden sections */
    /* padding-right intentionally removed — controlled per content div */
  }

  /* Consistent mobile typography baseline */
  html.is-mobile-snap #horizontal-strip > section p {
    font-size: 1.25rem;
    line-height: 1.75rem;
    margin-top: 0.5rem;
  }

  html.is-mobile-snap #horizontal-strip > section h2 {
    font-size: 3rem;
    line-height: 1;
    letter-spacing: -0.04em;
    margin-top: 0.75rem;
    margin-bottom: 1.75rem;
  }

  html.is-mobile-snap #horizontal-strip > section .text-eyebrow {
    margin-bottom: 1.5rem;
  }
</style>